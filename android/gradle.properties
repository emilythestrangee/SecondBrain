org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Gradle JDK configuration
# If you have a Java 17+ JDK installed, set JAVA_HOME to that path (recommended) and remove or comment out org.gradle.java.home below.
# Example (PowerShell as Admin):
# setx /M JAVA_HOME "C:\\Program Files\\Java\\jdk-17"; setx /M PATH "%JAVA_HOME%\\bin;%PATH%"
# After setting JAVA_HOME, restart your shell.
# If you prefer to set org.gradle.java.home directly, change the path below to your JDK 17 installation.
# org.gradle.java.home=C:\\Program Files\\Java\\jdk-17
# NOTE: Previously pointing to Android Studio's bundled JBR caused jlink errors; using a proper JDK 17+ (Temurin/Adoptium, Amazon Corretto, Zulu) is recommended.
